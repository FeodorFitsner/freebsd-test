init:
- whoami
- appveyor version
#- freebsd-version
- bash --version
- dotnet --version
- pwd
- env

test_script:
- sudo lsof -i -P -n | grep LISTEN
- bash enable-ssh.sh
#- ls -al ~/build-agent
- ls -al ~/projects

artifacts:
- path: README.md

build: off
